<!-- HEADER -->
<%- include('./includes/header.ejs') %>


<!-- SECTION HEADER -->
<!-- <main>
    <section class="firstSection">
        <h1>You have finally found what you were searching for!</h1>
    </section>
</main> -->

<section class="landingImage">
    <div class="landingContainer" style="font-size: 4rem; color: darkblue">
        <% if(name) { %>
            SpreadLove: <%= name %>! <i class="fas fa-child" style='font-size:5rem; color:red'></i><i class="fas fa-heart" style='position: relative; font-size:2.2rem; top: -4rem; left:-1rem; color:red; transform:rotate(20deg);'></i>
        <% } else { %>
            SpreadLove Every.ONE! <i class="fas fa-child" style='font-size:5rem; color:darkblue'></i>
        <% } %>
    </div>
    <% if(message) { %>
        <div class="message"><%= message %></div>
    <% } %>
</section>
<button class="btnPWAShowBanner" style="display:block; padding: 2rem 3rem; background-color: rgba(200, 200, 160, .7);">Installation banner anzeigen</button>

<!-- FOOTER -->
<%- include('./includes/footer.ejs') %>